{
  "include": ["**/*"],
  "exclude": ["node_modules"],
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // Environment setup & latest features
    "lib": ["ESNext"],
    "target": "ESNext"
  }
}
